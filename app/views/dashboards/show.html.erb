<div>
  <div class = 'title'>
    <h1>Hello <span><%= current_user.name %></span>, we love you.</h1>
  </div>

  <div class="tabs">
    <%= link_to "My events", dashboards_path(page: 'events') %>
    <%= link_to "My invitations", invitations_path(page: 'invitations') %>
  </div>
</div>

<div class="wrapper" data-controller="invitation">
    <div class="close-btn mt-2 hidden" data-invitation-target="close" data-action="click->invitation#close">X</div>
    <div class="rsvpLinks d-flex">
      <a href="#">Approve</a>
      <a href="#">Decline</a>
    </div>
      <% @invitations.each do |invitation| %>
     <div class="tab" data-invitation-target="tab" data-action="click->invitation#open">
      <h3>INVITATIONS</h3>
      <span><%= invitation.event.name %></span>
      <span><%= invitation.event.user.name %></span>
      <% end %>
      <div class = "invitation hidden" data-chat-target="content" >
        <%= render 'dashboards/invitation', :locals => { :event => @event, :invitation => @invitation } %>
      </div>
    </div>
  </div>


  <%# <div class="wrapper" data-controller="dashboard-invitation">
    <div class="close-btn mt-2 hidden" data-dashboard-invitation-target="close" data-action="click->dashboard-invitation#close">X</div>
    <div class="tab" data-dashboard-invitation-target="tab" data-action="click->dashboard-invitation#open">
      <div class="d-flex justify-content-between">
        <h3>Your invitations</h3>
      </div>
      <div class = "dashboard-invitation hidden" data-dashboard-invitation-target="content" >
        <%= render 'dashboards/invitation', :locals => { :event => @event } %>
      <%# </div> %>
    <%# </div> %>
  <%# </div> %>
<%# </div> %>
