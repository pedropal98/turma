<%= month_calendar(events: @events, attribute: :date) do |date, events| %>
  <%= date %>
    <% events.each do |event| %>
    <div class="event">
      <p><%= event.name %></p>
    </div>
  <% end %>
<% end %>
