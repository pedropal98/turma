<div class="container">
  <h1>invitations</h1>

  <div class="card-grid">
    <% @invitations.each do |invitation| %>
     <h1><%= invitation.user.name %></h1>
      <h1>Status:
        <span>
          <% if invitation.status.nil? %>
            <span class="pending">Pending</span>
          <% elsif invitation.status %>
            <span class="status">Approved</span>
          <% else %>
            <span class="declined">Declined</span>
          <% end %>
        </span>
      </h1>
      <div>
        <% if invitation.status.nil? %>
          <%= link_to "Approve", event_invitation_path(params[:event_id], invitation, status: true), method: :patch %>
          <%= link_to "Decline", event_invitation_path(params[:event_id], invitation, status: false), method: :patch %>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
